declare global {
  namespace App {
    // interface Error {}
    interface Locals {
      isAdmin: boolean;
    }
    interface PageData {
      isAdmin?: boolean;
    }
    // interface PageState {}
    // interface Platform {}
  }
}

export { };
